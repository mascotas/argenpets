define([
  "jquery",
  "underscore",
  "backbone"
  ], function($, _, Backbone){

    var MenuView = Backbone.View.extend({
      events : {
      },
      initialize: function () {
      },
      render: function(){
      }
    });

    return MenuView;
  });